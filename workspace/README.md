# Demo Workspace

This folder is the sandbox-style working directory used by the coding-agent prototype.

The included calculator project is intentionally small so the following commands can be demonstrated safely:

- `list files`
- `read calculator.py`
- `run tests`
- `check project`
